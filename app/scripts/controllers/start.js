'use strict';

/**
 * @ngdoc function
 * @name testingFrontendApp.controller:StartCtrl
 * @description
 * # StartCtrl
 * Controller of the testingFrontendApp
 */
angular.module('testingFrontendApp')
  .controller('StartCtrl', ['$scope',
  	function ($scope) {
  		$scope.init = function () {
  			//get the paper id
  		};
  }]);
